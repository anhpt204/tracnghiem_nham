package ktuan;

import java.util.Comparator;

public class ComparatorEarly implements Comparator<RequestTx> {
	public int compare(RequestTx o1, RequestTx o2) {
		return o1.get_posStart()._early-o2.get_posStart()._early;
	}
}
